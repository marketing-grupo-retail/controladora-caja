IF TS.LINETYPE = 18 THEN BEGIN
	IF TS.LINEDATA = 0 AND UE.HEADER% = 0 THEN 	\
		UE.HEADER% = 1				! APAGAMOS SI HEADER
	IF TS.LINEDATA = 0 AND UE.HEADER% = 1 THEN 	\
		BEGIN					\APAGAMOS SI HEADER
		CALL U.IMPRIMIR("LE ATENDIO: "+	\
		STR$(VAL(UNPACK$(TS.OPER$)))+" "+UE.NOMOPER$,4100H)
		UE.HEADER% = 0
		ENDIF
ENDIF
